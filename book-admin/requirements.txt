fastapi==0.111.0
uvicorn==0.30.1
SQLAlchemy==2.0.29
python-multipart==0.0.9
requests==2.32.3
werkzeug==3.0.3

# Optional (uncomment if you need these features server-side)
pandas==2.2.2            # Enable /export/csv endpoint
gspread==6.1.2           # Google Sheets integration
oauth2client==4.1.3      # Google auth helper for gspread

# Keep this file lean to reduce cold start size. Add extras only if required.
