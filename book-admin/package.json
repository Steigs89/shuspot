{
  "name": "book-admin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r public/* dist/ && cp index.html dist/",
    "start": "echo 'Static deployment'"
  }
}
