# Netlify redirects and proxies

# Proxy API to your backend. Replace the placeholder with your API base URL on Render.
/api/* https://YOUR-RENDER-API.example.com/api/:splat 200

# SPA fallback
/* /index.html 200
